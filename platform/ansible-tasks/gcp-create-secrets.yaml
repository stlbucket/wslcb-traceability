- name: create secret
  command: "gcloud secrets create {{PROJECT_ID}}-postgres-password --data-file=- --locations=us-central1"
- name: enable compute.googleapis.com
  command: "echo "tacos" | gcloud secrets versions add {{PROJECT_ID}}-postgres-password --data-file=-"
